"""
    파이썬에서 제공하는 자료

    collections 모듈
      deque : queue 개념 (양쪽이 열려있는 Queue 구조)
      defaultdict
      Counter
      namedtuple
      OrderedDict
      array 모듈 : 동일한 Data Type 을 가진다.  List = 여러가지 Data Type을 가진다.
      List는 array에 비해 메모리 사용이 비효율적이다.
      heapq 모듈 : List는 sort 함수를 이용하여 정렬. Data가 많을 경우 정렬 시간이 오래 걸린다.
                   heapq list데이터를 정렬된 상태로 유지.

      bisect 모듈 (정렬된 상태로 Item을 유지, 요소 추가, 중복값 처리)
      Queue
      struct
      copy
"""


